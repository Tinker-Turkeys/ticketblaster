<h1>Registrations#new</h1>

<%= form_for [@event, @registration] do |f| %>
  <% @event.custom_fields.each do |field| %>
  <% value = "yes" %>
    <%= custom_field_tag(field, value) %>
  <% end %>
  <%= f.submit "RSVP" %>
<% end %>

